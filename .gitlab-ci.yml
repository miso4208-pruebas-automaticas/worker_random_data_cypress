image: cypress/base:10

before_script:
  - npm install

stages:
  - test

cache:
  key: ${CI_COMMIT_REF_SLUG}

cypress:
  stage: test
  script:
    #- npm run test
    #- npm install cypress --save-dev
    #- npm run test
    - npm install
    - $(npm bin)/cypress run

  artifacts:
    paths:
      - /cypress/videos/
